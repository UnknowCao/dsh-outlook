# dsh-outlook bundle patch.
# Inserts one host-plane row: Outlook COM tools. The row publishes no service,
# so no realm is needed; it registers global tools every agent sees.

- insert:
    - id: outlook
      name: 'dsh-outlook'
